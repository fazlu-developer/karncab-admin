# KarnaCab Management Admin

Laravel 12 operations console. Users, drivers and dashboards read the shared platform MySQL tables directly. Flutter apps still call Nest.

```bash
php artisan serve --port=8001
```
